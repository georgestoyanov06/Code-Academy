#include <stdio.h>

void bit(unsigned int *a, int pos){
    unsigned mask=~(1<<pos);
    *a=*a&mask;
    return ;
    }




int main(void){
    unsigned i=15;
    bit(&i,1);

printf("%d\n",i);
return 0;

}
